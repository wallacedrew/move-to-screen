import XCTest
@testable import MoveToScreenDomain

final class PlaceholderTests: XCTestCase {
    func test_domain_module_compiles() {
        // Replaced by real tests starting in step 3.
        XCTAssertTrue(true)
    }
}
