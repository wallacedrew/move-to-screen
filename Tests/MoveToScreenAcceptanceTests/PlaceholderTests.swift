import XCTest

final class PlaceholderTests: XCTestCase {
    func test_acceptance_target_compiles() {
        // Replaced by ConsolidateScatteredTerminalWindowsTests in step 1
        // (which then stays red until step 7).
        XCTAssertTrue(true)
    }
}
